# godot-csharp-experiments

A lab of small, focused **C#** experiments in **Godot 4.6.2 (.NET build)** — the
C# sibling of [`water-kit`](../water-kit) (GDScript). Each technique is isolated
in one self-contained, self-validating scene. This repo is where the work that
genuinely benefits from C# lives: large stateful systems, zero-GC `struct` data,
compiler-checked interfaces/generics, and tight numeric loops.

## Prerequisites

| Tool | This project uses | Notes |
|---|---|---|
| **Godot** | `/Applications/Godot-mono.app` (4.6.2 **.NET**) | The **standard** Godot build can't run C#. Use the mono/.NET app. |
| **.NET SDK** | 8.0.x at `~/.dotnet` | On `PATH` via `~/.zshrc`. Verify: `~/.dotnet/dotnet --version`. |
| **Editor** | VS Code + C# Dev Kit | Godot has no built-in C# editor. |

## Open / build / run

```bash
# Open the project in the .NET editor
/Applications/Godot-mono.app/Contents/MacOS/Godot --path .

# Or build from the CLI first
~/.dotnet/dotnet build
```

The default run scene is `scenes/03_mna_wave.tscn`. Any scene can be launched
directly (`Godot-mono … res://scenes/NN_<name>.tscn`) or rendered to a PNG via the
screenshot harness (`tools/shoot.tscn` — see `CLAUDE.md`).

## Scenarios

| # | What it shows | Source | Status |
|---|---|---|---|
| 03 | `mna_wave` — **MNA stamp solver on the GPU, matrix-free** — C# port of water-kit scene 25. Same shared kernel (`shaders/mna_wave.glsl`) + display shader; only the host is reimplemented in C#. Every cell a node / edge a pipe / cell a capacitor → the implicit damped-wave system, relaxed by K **Jacobi sweeps/tick** ping-ponged as compute dispatches on the reusable **`GpuStampSolver`** (physics is a swappable GLSL stamp). Backward-Euler ⇒ unconditionally stable. First scene on the new C# `DemoUI`; auto-pokes + left-click. A **backward-Euler ↔ Crank-Nicolson** scheme slider tunes the numerical dissipation (BE damps ripples for unconditional stability; CN shares the stiffness with the past level → ripples ring) — live on Jacobi/RBGS | [water-kit 25](../water-kit/scripts/scene_25_mna_wave.gd) | ✅ verified |
| 04 | `stamp_flow` — **Phase 2: ideal circuit sources on the grid** — same `GpuStampSolver` + damped-wave medium as 03, but the stamp (`shaders/stamp/stamp_flow.glslinc`) places the two MNA sources on the grid: a **faucet** = current source (persistent Gaussian → rhs) and a **drain** = Dirichlet pin = ideal voltage source (a corner held to a fixed level). Relaxation settles to the steady flow field — a tilted gradient from the faucet mound down into the drain pit (the scene-20 resistor network at grid scale). "Keep the solver, change the circuit": only the stamp path differs from 03. Left-click aims the faucet. Jacobi/RBGS only (CG/multigrid inline the wave operator) | [plan §5](docs/gpu-stamp-solver-plan.md) | ✅ verified |
| 05 | `stamp_heat` — **Phase 3a: implicit heat / diffusion** — the cheap "same solver, new circuit" proof: identical `GpuStampSolver` + surface shader as 03/04, only the stamp (`shaders/stamp/stamp_heat.glslinc`) changes. Backward-Euler diffusion (unconditionally stable); a persistent hot source + a Dirichlet pin (fixed-temperature spot), shown as the divergent temperature colormap (blue cold · white ambient · red hot). Where the wave stamp ripples and flow settles, heat spreads **monotonely** — same steady screened-Poisson operator, different transient. The `inertia·(h_curr−h_prev)` term is a real thermal-memory knob that also keeps the `h_prev` binding alive. Jacobi/RBGS | [plan §6](docs/gpu-stamp-solver-plan.md) | ✅ verified |
| 06 | `stamp_reverb` — **Phase 3b: 2D plate reverb** — the literal audio-DSP analog behind the whole "stamp = circuit" thesis. Same `GpuStampSolver`; the stamp (`shaders/stamp/stamp_reverb.glslinc`) is the damped-wave operator turned into a PLATE: **clamped Dirichlet-0 edges** (the frame — reflections build the plate's standing modes) + very low damping (a long, ringing reverb tail). Strikes excite many modes that interfere and decay, shown on the divergent colormap (via a new additive `colormap_gain` uniform) so the +/- pattern and nodal lines read. Auto-strikes + left-click. Jacobi/RBGS | [plan §6](docs/gpu-stamp-solver-plan.md) | ✅ verified |
| 07 | `stamp_fluid` — **Phase 3c: pressure-projection fluid (Stam stable fluids)** — the problem that does NOT fit the single-scalar stamp solver, so it's a purpose-built `scripts/lib/FluidSim.cs`: advect velocity (semi-Lagrangian) → divergence → **∇²p = div via the SAME matrix-free Jacobi relaxation** the stamp solvers use → subtract ∇p (divergence-free) → advect buoyant dye → curling smoke. 6 fluid kernels (`shaders/fluid/`), a smoke display shader (`fluid_smoke.gdshader`), no-slip walls. Left-click injects; auto plume for the shot. **"Same solver, bigger circuit."** | [plan §6](docs/gpu-stamp-solver-plan.md) | ✅ verified |
| 08 | `blob3d` — **Phase 4: the stamp solver in 3D, no ray tracing** — `GpuStampSolver3D` (image3D, 7-point stencil, 48³) relaxes a 3D implicit damped-wave blob field; each frame the volume is read back and **marching-tetrahedra**'d (`scripts/lib/IsoSurface.cs`) into a triangle mesh Godot rasterizes and lights as a slimy blob. A central source holds the shape, auto-pokes wobble it. The stamper generalizes to 3D almost for free — the real work is turning a volume into rasterizable geometry, which is exactly what the C# track is for. | — | ✅ verified |
| 09 | `fluid3d` — **Phase 4: 3D pressure-projection fluid → GPU particles** — `FluidSim3D` (full 3D Stam sim on a 48³ grid; the stamper does the 3D Poisson pressure projection each tick) drives a buoyant plume. The velocity volume is read back and CPU-advects 12k MultiMesh billboard particles (soft radial puffs, warm age palette) → swirling 3D vortices, no ray tracing, ~115 fps. Scene 07's solver, one dimension up. | — | ✅ verified |
| 10 | `fog3d` — **Phase 4: 3D fluid → volumetric FogVolume** — the same `FluidSim3D` density shown as real volumetric smoke: a `Texture3DRD` wraps the RD density texture and a `shader_type fog` shader samples it inside Godot's froxel volumetrics — **no readback, no custom ray marching** (the sim texture feeds the renderer directly). Soft by nature (froxel resolution), but the stamper-driven plume reads. The "lean on the engine" display path. | — | ✅ verified |
| 11 | `smoke_world` — **smoke drifting in a world** — the scene-09 particle fluid embedded in a lit environment (dark dusk sky, ground, low warm sun, a varying breeze at the source) so the plume wafts and curls like glowing embers rising in air. 14k MultiMesh embers advected by `FluidSim3D`; the sim box sits on the ground inside a much bigger world. Particles chosen over the FogVolume — froxel fog reads too soft/low-contrast in an open lit scene. Limit: the smoke only lives in the fixed 48³ box. | — | ✅ verified |
| 12 | `fog_machine` — **movable fog-machine rig** — a fog machine on the floor emitting `FluidSim3D` smoke; the machine + particles + a wireframe outline of the bigger (8u) sim box all parent under one movable rig. The sim runs in its fixed 48³ grid; the rig transform places the volume in the world, so **left-drag on the floor carries the whole fog box around the scene** (auto-drifts when idle). Overdraw lesson: additive billboards in a big screen-filling box tank fill-rate — small quads + moderate count hold 60 fps. | — | ✅ verified |
| 13 | `ameye_water` — **stylized water, built stage by stage** — C# port of water-kit scene 08 (`scene_08_ameye_water.gd`), ported from the URP Shader Graph asset. The 176-line `ameye_water.gdshader` is **portable verbatim**; only the 144-line builder is reimplemented in C# (`AmeyeWater.cs`). Six stages, **each a toggle in the panel**: (1) Gerstner surface (4 layered directions) + correct normals, (2) depth fade, (3) HSV shore color, (4) foam, (5) blended panning normal maps + screen refraction, (6) toon specular. Half-submerged rocks on a sandy bed give the depth fade / refraction shorelines to read against. Screenshot-verified against water-kit's reference. | [water-kit 08](../water-kit/scripts/scene_08_ameye_water.gd) · [ameye.dev](https://ameye.dev/notes/stylized-water-shader/) | ✅ verified |
| 14 | `composite_gerstner` — **multi-band composite water** — scene 13's exact ameye look, but the fixed 4-wave same-wavelength sum is replaced by a real **composite field — long swell + mid + fine chop** — for a visibly richer, rolling sea. The CPU field is the C# `CompositeGerstner : GerstnerField` (`scripts/lib/`, ported from water-kit's `composite_gerstner.gd`), feeding N array-driven bands to `ameye_water_composite.gdshader` (the ameye shader with its 4 fixed waves → an N-band loop) via `wave_time` — the classic "layering is what makes Gerstner not-suck." Live **wave amp / speed** sliders rescale all bands. Extracted from the field buried in water-kit scene 100 into a clean water-on-a-seabed scene. | [water-kit 100](../water-kit/scripts/scene_100_boat_composite.gd) · [ameye.dev](https://ameye.dev/notes/stylized-water-shader/) | ✅ verified |
| 18_milk | `18_milk_coffee` — **milk in coffee (fluid-in-fluid, §7 first build)** 🔥 — the Stam pipeline of scene 07 plus the three things that make dye read as MILK: **signed Boussinesq drift** (`fs_add_milk` — force ∝ concentration, so it's heavier-then-lighter as it mixes), **MacCormack/BFECC second-order dye advection** (`fs_maccormack`, limiter-clamped — filaments stay filaments; a panel toggle A/Bs it against plain semi-Lagrangian), and **implicit Jacobi viscosity** (`fs_diffuse_vel` — cream-thick, unconditionally stable). Pour = dye + radial splash; drag = stir along the mouse. Coffee-absorption rendering with gradient-relief fold lighting + ceramic cup mask. 256² @ 120fps. All three extras are additive FluidSim options — scene 07 unchanged. | [plan §7](docs/mna-next-steps.md) | ✅ verified |
| 32_milk3d | \`32_milk_3d\` — **milk in coffee, 3D glass** 🔥 — §7 lifted to a volume: milk poured from above SINKS through clear coffee (signed Boussinesq, force ∝ concentration), mushrooms and finger-instabilities off the plume tip, raymarched (\`milk_glass.gdshader\`: front-to-back march in a cylinder mask, Beer–Lambert coffee tint, top-light occlusion, dilute-knee so haze stays translucent). 3D MacCormack (\`f3_maccormack\`, 8-corner limiter) + 3D implicit viscosity (\`f3_diffuse_vel\`) as additive FluidSim3D extras, and the §10 pressure swap as a live A/B: **deep multigrid vs Jacobi projection** in the panel. 56×84×56 @ 120fps. Click pours at the clicked spot. | [plan §7](docs/mna-next-steps.md) · solver-ledger §10 | ✅ verified |
| 24_base | `24_base` — **the frozen reference for the DSP experiment series**. Scene 24's pool, ball and camera; full sim (`ExpWaterSolver` — no MNA stamp anywhere in this series); one drive source; no obstacles; every experiment uniform at its no-op default. Raytracing OFF by default so the fps is the solver's, not the renderer's. The baseline is shared *by reference* (`scripts/lib/ExpPoolScene.cs`) rather than copied, so "one isolated variable per scene" is enforced by the code | [dsp-experiments](docs/dsp-experiments.md) | ✅ runs |
| 24_col | `24_col` — **isolated variable: ONE COLUMN**. Shape (cylinder / square / blade), radius, position, and **hard↔soft as a continuous slider** — 1 = perfect no-flux reflector (a solid neighbour's height is replaced by the centre's, so there is no gradient across the face: scatters, diffracts, casts a shadow), 0 = pure absorber (reads as a hole). Columns are rasterized host-side into the solver's obstacle mask AND drawn as matching geometry, so what the water reflects off is what you can see. Entirely linear | [dsp-experiments](docs/dsp-experiments.md) | ✅ runs |
| 24_cols | `24_cols` — **isolated variable: COLUMN COUNT**, 1→8, with grid / arc / scatter layout. Shape, radius and hardness pinned so only the count varies; layout is a preset because free placement would be a second variable. Still entirely linear — each column multiplies the scattering paths | [dsp-experiments](docs/dsp-experiments.md) | ✅ runs |
| 24_src | `24_src` — **isolated variable: SOURCE COUNT**, 1/2/3, plus spacing and a simultaneous↔staggered fire mode. No obstacles, nothing nonlinear: the pure superposition scene. The interference fringes are produced entirely by ADDITION, which at small amplitude is exactly what water does — worth seeing before any nonlinearity is added, since it sets the bar those experiments have to beat | [dsp-experiments](docs/dsp-experiments.md) | ✅ runs |
| 24_cxm_taps | `24_cxm_taps` — **isolated variable: TAP COUNT + POSITION**, 4→8, ring / line / scatter. The seam where the audio DSP becomes spatial: in a reverb Dattorro's extra output taps only decorrelate the stereo image, but injected into water **each tap is a PLACE**, so tap count is the spatial resolution of everything the tank contributes. Tank tuning pinned so only tap geometry varies. Taps 0–3 are the source's four raw nodes bit-identical; 4+ are extra cubic-Hermite reads of the same tank delay lines — the patch says "simplified, full Dattorro uses 7 taps per channel", so the paper's offsets are **not** in the source and are not invented. On `ExpPoolScene`, so columns work in it | [dsp-experiments](docs/dsp-experiments.md) | ✅ runs |
| 24_cxm_field | `24_cxm_field` — **isolated variable: HOW ~8 SCALARS BECOME A 2D FIELD** — the question the whole plan rests on. A tank hands you 8 numbers per tick; water needs 65k cells, and that expansion is a *separate* cost from running the tank. **SIM DROPS** injects taps and lets the full solver spread them (the expensive thing does the expansion — 256²×4 passes + a CFL-bound substep loop). **MODAL BASIS** switches the solver **off entirely** and evaluates `h = Σ aₙ·cos(mₙπx)·cos(kₙπz)` analytically per cell — the low-order basis from [path-a §4](../shorewaves/docs/design/path-a-cxm-tank.md), zero memory (no basis textures), no time stepping, no neighbours, no CFL, no substeps: a function evaluation rather than a recurrence, 256²×N one pass. **BOTH** layers the basis over a live sim. Costs for both print on the readout | [path-a §4](../shorewaves/docs/design/path-a-cxm-tank.md) | ✅ runs |
| 24_cxm_clock | `24_cxm_clock` — **isolated variable: THE TANK CLOCK**, 300 Hz → 48000 Hz. The single adaptation made to `cxm-1978.cmajor`, swept end to end: at 48000 it is the audio patch bit-for-bit (longest delay 7188 samples = 150 ms, reads as ringing); at 3600 the same samples stretch to ~2 s; at 300, ~24 s. The incommensurate ratios 7188/6005/6807/5106 that stop the modes phase-locking survive untouched — which is why it is a clock change and not a retune. The real Lexicon 224 had this control too. Readout shows the longest delay in seconds | [cxm-1978](../neptunely_standalone/js/audio/cmajor/fx/cxm-1978/cxm-1978.cmajor) | ✅ runs |
| 24_cxm_type | `24_cxm_type` — **isolated variable: THE SOURCE'S OWN PRESET TABLES**. Not invented knobs — Type (Room `decayScale 0.7/gDecay 0.60/0.45`, Plate `1.0/0.70/0.50`, Hall `1.3/0.75/0.55`), Diffusion (0.4/0.7/0.9 scaling the 4-stage input diffuser) and Tank mod (depth 4/12/24 at 0.5/1.0/2.5 Hz), applied exactly as the patch defines them. Tank mod matters most here: modulating delay length is what stops the pattern freezing into one standing wave | [cxm-1978](../neptunely_standalone/js/audio/cmajor/fx/cxm-1978/cxm-1978.cmajor) | ✅ runs |
| 24_zita | `24_zita` — **isolated variable: REVERB TOPOLOGY**, figure-of-eight ↔ FDN. `scripts/lib/ZitaFdn.cs` is a faithful port of [`fx-zitareverb.cmajor`](../neptunely_standalone/js/audio/cmajor/patches/fx-zitareverb.cmajor): 8 delay lines, both delay tables verbatim, `DelayWithFeedback` allpasses at c = ±0.6, the RT60/damping shelf, the 12-butterfly Hadamard `shuffle()` and its `g = √0.125` orthonormal scaling. Dattorro is **hardwired to 2** cross-coupled chains giving 4 raw nodes; an FDN's channel count is set by its mixing matrix, so it hands out **8 distinct channels natively**. For water each channel is a PLACE — the question is whether N-way orthogonal mixing buys anything spatially. Same clock treatment as CXM | [fx-zitareverb](../neptunely_standalone/js/audio/cmajor/patches/fx-zitareverb.cmajor) | ✅ runs |
| 24_move | `24_move` — **isolated variable: OBJECT MOTION AS A SOURCE**. The object PUSHES water (`volume_in_sphere` raises where it was, lowers where it is) but never blocks anything. So the OPERATOR never changes — the medium is untouched and only the forcing moves, which is exactly why a moving source stays cheap and bakeable (one kernel plus a track under shift-invariance) where a moving wall does not | [dsp-experiments](docs/dsp-experiments.md) | ✅ runs |
| 24_scat | `24_scat` — **isolated variable: OBJECT MOTION AS A SCATTERER**, and the most informative scene in the series. Same paths and speeds as `24_move`, but the object BLOCKS waves — it lives in the obstacle mask as a no-flux wall, so moving it means re-rasterizing and re-uploading that mask every frame (~61 uploads/s on the readout). That **changes the operator every step**, breaking the time-INVARIANT half of LTI: a measured IR cannot represent it at all, and not because anything is nonlinear — there is simply no longer one fixed system to measure. This is the boundary the whole IR/CXM direction runs into | [dsp-experiments](docs/dsp-experiments.md) | ✅ runs |
| 24_irlen | `24_irlen` — **isolated variable: IR KERNEL LENGTH**, 64 → 1024 slices (16 MB → 268 MB at 256², the practical ceiling), rebuilding and re-capturing on change. On `ExpPoolScene`, so unlike the original `24_ir` it shares the frozen baseline and can have obstacles — capturing an IR *with* a column is the interesting follow-on, since a static scatterer is completely free in a linear operator. Given "nothing needs to ring": if a short kernel looks like a long one, the freed memory is what makes MULTI-SOURCE affordable — 8 sources at 64 slices costs less than one at 512 | [path-e](../shorewaves/docs/design/path-e-bake-and-replay.md) | ✅ runs |
| 24_modal | `24_modal` — **Path E finished: measure → FIT → resonators.** `24_ir` works in sample space (whole-field slices, 268 MB of FIR); [path-e §2](../shorewaves/docs/design/path-e-bake-and-replay.md) says that is the wrong space. CAPTURE projects each tick onto the analytic basis so the response becomes 8 scalar series instead of 8 whole fields; FIT extracts each mode's ω (dominant DFT bin) and σ (log-envelope regression **from the peak onward** — regressing across the initial rise reports σ≈0 and puts every resonator pole on the unit circle); REPLAY is a bank of 2-pole resonators, solver OFF, **two floats of state per mode and no stored kernel at all**. Fit table printed live. Tests H-E1/E2/E3 | [hypotheses](docs/hypotheses.md) · [path-e](../shorewaves/docs/design/path-e-bake-and-replay.md) | ✅ runs |
| 24_cxm_loop | `24_cxm_loop` — **Path A finished: the CLOSED water→tank→water loop.** [path-a §4](../shorewaves/docs/design/path-a-cxm-tank.md) specifies a feedback architecture — sponge energy is the tank's *input*, taps are the return — but every earlier CXM scene built the return only, with the tank driven by the same excitation as the sim (a parallel voice, not a loop). Here the SEND is real: a GPU reduction sums \|h\| over the outer annulus into a 16×16 tile texture so the readback is 1 KB rather than 1 MB/frame, normalised to a **mean** so "loop gain" is a true ratio. Controls: close/open the loop, gain, annulus width, and **Drive OFF** — the actual test of whether it sustains. Tests H-A1 | [hypotheses](docs/hypotheses.md) · [path-a](../shorewaves/docs/design/path-a-cxm-tank.md) | ✅ runs |
| 24_CXM | `24_cxm` — **scene 24 + full sim + a faithful CXM-1978 reverb** — forked from scene 24 verbatim; the MNA stamp is replaced by the FULL SIM (`WebgpuWaterSolver`) and a 1:1 port of [`cxm-1978.cmajor`](../neptunely_standalone/js/audio/cmajor/fx/cxm-1978/cxm-1978.cmajor) (`scripts/lib/CxmTank.cs`) runs beside it — Dattorro figure-of-eight, 4-stage input diffuser (229/173/612/447), cross-coupled chains, modulated delays 90° apart with cubic Hermite reads, exact tank delays 7188/6005/6807/5106, split-band decay. **Only the CLOCK is adapted:** the source's 48 kHz makes the longest delay 150 ms (ringing, not water), so the clock is a slider — every delay length and ratio stays exactly as written, and at 3600 Hz the longest delay is ~2 s. One excitation feeds sim AND tank; the tank's 4 raw nodes stamp back at 4 tap positions (in audio the extra taps buy stereo decorrelation — here each tap is a PLACE). Tank toggle = the A/B; raytrace toggle separates render cost from solver cost | [cxm-1978](../neptunely_standalone/js/audio/cmajor/fx/cxm-1978/cxm-1978.cmajor) · Dattorro JAES 1997 | ✅ verified |
| 24_IR | `24_ir` — **scene 24 measured once, then replaced by convolution** — the same fork, but the sim is captured and retired. CAPTURE fires ONE impulse and records 512 height fields into an `image3D` (128 MB) — h(x,y,k), the pool's impulse response. REPLAY switches the solver **off entirely** and rebuilds the surface as `SUM_k s[t-k]*h(x,y,k)`: no neighbours, no stencil, no CFL, every cell independent — a recurrence replaced by a function evaluation. Drive signal is a live control (impulse train / swell / noise / manual), which is the point: **an IR is an OPERATOR, not a recording** — one measurement, infinitely many inputs, different outcomes. Tap list is compacted to the non-zero signal history, so cost tracks how dense the drive is (7/512 taps for an impulse train). Modes: Replay · Sim (A/B) · Inspect raw IR slice k. Exact only while the response is linear | [path-e](../shorewaves/docs/design/path-e-bake-and-replay.md) | ✅ verified |
| 26 | `26_shore_base` — **the solver-equivalence reference scene** — [path C](../shorewaves/docs/design/README.md) run for real. The 24-series baseline is Wallace's damped heightfield; this one is **untouched KP07** (`ShallowWaterKp`) on a shoreline worth approximating: `ShoreScenario`, 92.2 m at 768² / dx 0.12, a **curved waterline** (two incommensurate meanders → bay + headland), a **Dean equilibrium profile** `h = A·d^(2/3)` instead of a straight ramp, and a **longshore sandbar** at 16 m so swell breaks offshore, reforms in the trough, and breaks again at the beach. Boundary mode 2: west wavemaker + relaxation zone, absorbing strips on E/N/S (a meandering shore reaches the N/S edges — mirror walls would stand up a longshore reflection in seconds). Cost is held ≈ constant against scene 17 by matching the CFL number (0.136 vs 0.137), so 2.6× the wavelengths cost 1.6× the cells. **Solvers are independent toggles** — KP07 starts ON, every candidate starts OFF and gets built one at a time, so a cheap solver is A/B'd against the reference on the same bed in the same frame. **Surface is Flat vs Styled**: flat is the raw height field (where you judge a solver), styled is the full water shader (where you judge shading) — one click apart so the two failure modes can't be confused. Self-checks the shelf is flat under the relaxation zone (a sloped bed there reflects the wavemaker invisibly). 115 fps · sim ×1.00 · 3 substeps · nan 0 | [design/README](../shorewaves/docs/design/README.md) | ✅ verified |
| 50 | `boat_mna` — **boat + camera-following CN-MNA reactive window** — the [mna-next-steps](docs/mna-next-steps.md) §2a/§2.5 build, forked from scene 16: `h = gerstner + mna`. A 48 m **toroidal sim window rides the boat** (clipmap scrolling: world-anchored field, wrap-addressed RBGS via `solve_rbgs_wrap.glslinc` + an additive body-override arg on `GpuStampSolver`; the wrap seam always sits in the sponge). **Absorbing sponge edges** ramp damping *and* leak (velocity damping alone can't kill a statically-forced mound — found via field min/max logs), **edge fade** melts MNA into pure Gerstner, the boat's wake is a **balanced bow/stern dipole** (zero net volume, v²-scaled, 4-poke `stamp_wave_multi.glslinc`), and a **reactive foam buffer** (`foam_update.glslinc`: \|∇²h\| above ambient-ringing threshold → accumulate+decay, toroidal) breaks up through the ameye foam texture. Sub-step oversampling slider, CN θ, residual readout, debug colormap; left-click pokes, auto-cruise demo. | [plan §2a/§2.5](docs/mna-next-steps.md) | ✅ verified (live 26 s) |
| 51 | `singularity_sea` — **the sea foams itself** — fork of 50 (50 untouched; own script + forked foam kernel). Ambient layers, each toggleable: **phase-singularity whitecaps** (`singularity_mask.glslinc` — the Gerstner band sum read as a phasor field; 2×2 winding finds the drifting defect points where bands cancel → foam v2 whitecap source; the **amp-flatten slider is a sea-state dial** from lone caps to storm chowder), **eddy-core foam** (`eddy_mask.glslinc` — the same winding primitive on the swirl fluid's velocity angle, speed-gated), **ambient vortex primitives** (`vortex_inject.glslinc` — an analytic world-lattice of regularized Kirchhoff eddies, checkerboard circulation, injected pre-projection so the fluid makes them real). Zero new physics state — structure read out of fields the scene already computes: the "simulation telemetry as design material" thesis, working. | [lift list](docs/science-boi-lift.md) | ✅ verified |
| 49 | `fhn_spirals` — **FitzHugh–Nagumo excitable media + spiral-tip tracking** — science_boi lift: two-field explicit reaction–diffusion on a 256² bare plate (`fhn_step.glslinc` — the stamp scaffolding's stencil with a reaction RHS, no solver), the classic **cross-field spiral seed**, and the **winding-number tip detector** (`fhn_tips.glslinc` — third use of the shared defect primitive): tips glow red = CW / orange = CCW. Left-click excites; poke a wave's refractory tail to break the front and birth rotors. ε/a/b/D/dt/substeps live. | [science_boi](../science_boi/src/bin/chiral_spirals.rs) · [lift list](docs/science-boi-lift.md) | ✅ verified |
| 52 | \`boat_batty\` — **Batty variational cut-cell shore boundaries** (isolated scene-50 clone; Batty–Bertails–Bridson 2007, the compass-report pivot) — every neighbour pipe's conductance is weighted by the sub-grid OPEN FRACTION of the cell face (\`stamp_wave_batty.glslinc\`: analytic island-waterline SDF at face midpoints, baked circles; the stamp stays an SPD weighted Laplacian). **Waves finally see the islands**: single-impulse A/B proves it — mode off, the ring rides straight OVER the skerry; mode smooth, it breaks against the shore and reflects. Solid cells are excluded from the system (force masked by open fraction + hard leak — a poke gaussian into a disconnected cell double-integrates unbounded, found the fun way). Panel: Off / Smooth / Binary-staircase + shore softness. Skerry parked near spawn for instant reflections. Lesson en route: a ringing (CN, damping-0) medium under ANY stationary periodic forcing accumulates resonantly — demo pokes must be impulses or move. | [Batty 2007](https://www.cs.ubc.ca/~rbridson/docs/batty-siggraph07-fluidsolids.pdf) | ✅ verified (A/B shots) |
| 53 | `underwater_lab` 🔥 — **the water VOLUME as a rasterised MASK**, replacing every underwater boolean. Scene 50 stripped to water + flycam (no boat), then a SubViewport renders a box whose top face is displaced by the SAME `uw53_displace()` the water shader uses — shared `.gdshaderinc`, so composite Gerstner + the MNA window + the foam puff all apply identically and cannot drift. Front faces write black, back faces white; depth testing means OUTSIDE the volume the front face wins (0) and INSIDE only back faces remain (1). Everything below-water is then MULTIPLIED by that mask — fog, meniscus, the underside branch — instead of switched by a camera test. **Four invariants**: one wave function, one data push, matching vertex grids (same footprint AND subdivisions — a coarser mask straight-lines across short waves), same clock same frame. | proven in water-kit `213_minimal` | ✅ verified |
| 250 | `squish` — **residual divergence as compressibility** — the proof of concept for the [artifact series](docs/artifacts-250.md): a truncated pressure projection isn't a failure to be incompressible, it's a *material*. Scene 18's pipeline verbatim (`FluidSim` + `fs_add_milk` + MacCormack + implicit viscosity), re-skinned into the series palette and re-pointed at its own residual. **Jacobi K is the only dial**; the A/B is the same solver at K=400 (hard-capped — K ∝ N² is 1600 sweeps at 1024², which is a freeze, not a comparison, and the readout says `capped` when it bites). Artifact view = the leftover \|∇·v\| in `#E23D6D`, which needed `FluidSim.MeasureResidual` — the existing `_div` holds the solve's *right-hand side*, not what truncation left behind. Unlike 18 the domain is honest about being a **vertical slice**: `fs_add_milk`'s drift term is in-plane buoyancy, so rendering it top-down (as 18 does, cup rim and all) makes gravity an arbitrary sideways bias. Establishes `Scene250Base` (standard panel, TIME scale, grid dropdown carrying the similarity table) + `artifact_view.gdshader`. | [artifacts-250](docs/artifacts-250.md) · [log](docs/artifacts-log.md) | ✅ verified (live, tuned) · values baked at 1024²; not re-tuned since the side-view flip |
| 250b | `squish3d` — **the same artifact, one axis up** — the 2D→3D lift, and a *literal* one: `f3_add_source` does `v.y += dt·buoy·dye`, the identical term 250's slice is built on, so the box is that slice plus an axis (N × 1.5N × N = scene 32's 56×84×56 at the reference grid). **The A/B gets stronger in 3D**: the reference is the deep multigrid path `FluidSim3D` already carries, not more Jacobi — and MG's *cycle count* is grid-independent, so the honest solve costs the same at 40³ or 72³ and needs no cap. 250's weak reference is a 2D-only problem. Artifact view marches the `\|∇·v\|` volume as a second channel in `#E23D6D` (`artifact_volume.gdshader`, forked from `milk_glass` which stays untouched), plus a residual-solo mode. This is the experiment that seeded the whole series — scene 32's own comment records the verdict that Jacobi's under-converged squish reads *better* than the accurate answer; here it's named and given knobs. Fly camera on `Scene250Base` (RMB look · WASD · Q/E), so the orbit rig yields while flying. **The finding, stated plainly: the accurate solve LOSES.** Ryan's verdict on the multigrid reference was "sucks horribly" — under-converged projection reads as soft billowy cushioning, the honest answer reads harsher. That is the thesis of the whole 250 series, reproduced in the scene built to name it. | [artifacts-250](docs/artifacts-250.md) · [log §1](docs/artifacts-log.md) · [scene 32](scripts/Milk3D.cs) | ✅ verified (live, tuned — Copy-values baked) |
| 251 | `bounce` — **over-relaxation overshoot as elasticity** — 250's fluid, one solver step different, and the first true formula swap into the template. Red-black Gauss-Seidel with SOR (`fs_pressure_rbgs.glslinc`, PARITY compiled in ⇒ two pipelines, ping-ponged, off-colour copied through — the `solve_rbgs.glslinc` pattern): `p ← p_old + ω(p_GS − p_old)`. **ω is the whole instrument** — below 1 under-relaxes into a pillow that absorbs a push, above 1 every update overshoots and is pulled back, leaving a correlated over/under pattern that rings rather than squashes. Where 250 leaves a *deficit*, this leaves a *ringing*: compare the residuals' texture, not their amount. Reference is optimal-ω SOR run long (`ω* ≈ 2/(1+sin(π/N))`, `K ∝ N` not `N²` — so the accurate side is affordable, unlike 250's capped "more Jacobi"). `FluidSim` gained an opt-in `sor:` path; Jacobi untouched, so 07/18/250 stay byte-identical. ω rides in the existing sim push constant's third float, which the divergence and gradient passes declare as pad — no host-side layout change. | [artifacts-250](docs/artifacts-250.md) | ✅ verified (live) |
| 251b | `bounce3d` — **elasticity, one axis up** — 251 lifted the way 250→250b was. One thing does *not* survive by inspection and it is the whole content of the port: **the red-black parity must sum all three axes** (`(x+y+z)&1`). A 2D `(x+y)` split does not two-colour a 6-neighbour lattice — half the "black" cells end up adjacent and the pass reads values it is concurrently writing. The stamp solver met this first; `solve_rbgs.glslinc` carries the same warning. `FluidSim3D` now holds **three** solvers, all reachable: Jacobi (250b's artifact), red-black SOR (this scene's), deep multigrid (reference for both) — which makes this reference the honest one, converged at grid-independent cost with no cap needed. Opens on 250b's tuned fluid and tier so flipping between them isolates the solver. | [artifacts-250](docs/artifacts-250.md) · [log §4](docs/artifacts-log.md) | ✅ built · not yet tuned live |
| 256 | `plaid` — **checkerboard residual as woven texture** — no new kernel: 251's RBGS at ω=1 stopped after 1–2 sweeps. Red-black relaxes two interleaved sublattices in turn, so they are *never* equally converged — that asymmetry IS Gauss-Seidel, and truncating it freezes a **two-cell weave** into the residual. K=1 is maximum plaid; by K=8 the sublattices equalise and it dissolves into plain squish, the dial destroying its own artifact. Two firsts, both because the artifact is exactly two cells wide: **pixel-exact `texelFetch` sampling** (a two-cell checkerboard through a bilinear sampler is not faint, it is *flat grey* — the artifact vanishes into the sampler) and a **coarse default grid** (256², with 128² available) because here the grid dropdown is an *aesthetic* knob, not a fidelity one. **Result: the reference wins.** Ryan preferred the converged solve — the inverse of 250b's verdict, with the same fluid, and the observation that produced the material-vs-noise law. No 3D scene, deliberately: a raymarch low-passes away exactly what pixel-exact sampling exists to preserve. | [artifacts-250](docs/artifacts-250.md) · [log §1–2](docs/artifacts-log.md) | ✅ verified (live) — artifact judged unwanted, which is a result |
| 253 | `memory` — **pressure hysteresis as viscoelasticity** — the warm start turned into an instrument. Every Stam solver seeds this frame's pressure with last frame's, and with a *truncated* solve some of it survives — normally an invisible implementation detail. Here it gets a dial: `p₀ ← μ·p_prev` (`fs_pressure_warm.glslinc`, an in-place scale — safe precisely because there are no neighbour reads, unlike the RBGS pass). μ=0 forgets every frame, μ≈1 is the status quo 250 already runs, **μ>1 is a genuine positive-feedback loop** — the fluid pushes back against a *history* of being squeezed, which is what recoil is. Slider stops at 1.05 and the kernel rails the magnitude so the self-exciting range stays explorable instead of needing a relaunch. K is deliberately low: memory only exists while the solve is truncated. **Built as a test of the material-vs-noise law** — its residual is smooth and convects, so the law predicts it works, where plaid's lattice-pinned checkerboard did not. | [artifacts-250](docs/artifacts-250.md) · [log §1](docs/artifacts-log.md) | ✅ built · not yet judged |
| 257 | `eq` — **a designed residual spectrum: the instrument** — the one scene where the artifact's *shape* is drawn rather than inherited. In the cosine basis the Poisson operator is diagonal, so the exact projection is one divide per cell and any curve can be applied to it: `p̂(k) = W(k)·p̂_exact(k)`, `W = lowpass(knee, slope) × notch(centre, width, depth)`. **Cosine and not a complex FFT on purpose** — every pass in the fluid addresses neighbours with `clamp(c+off, 0, mx)`, i.e. Neumann walls, and the DCT diagonalises exactly that; an FFT diagonalises the *periodic* operator and would wrap the left wall into the right one as a seam (the argument `shaders/stamp/dct_1d.glslinc` makes at length). **This is the answer to 256**: the material-vs-noise law was a finding you had to live with, and here it is two sliders — keep long wavelengths under-projected (compliance, the good part of squish), leave short ones fully projected (no grid-frequency junk, the bad part of plaid). **The reference is EXACT** — `W(k)=1` *is* the true projection because the transform pair is exactly invertible; no other scene in the series can say better than "converged". The knee is a **world wavelength**, making it the series' only fully grid-proof artifact knob. Direct O(N²)-per-axis transform (the `dct_1d` v1 call): ~5 ms at 512², so the ladder stops there; the O(N log N) butterfly port is the known upgrade. **Verified live**: reference-on drives the residual to essentially black, which is the proof the transform pair inverts, and the knee/tilt produce visibly distinct fluids. | [artifacts-250](docs/artifacts-250.md) · [log §1b](docs/artifacts-log.md) | ✅ verified (live) — materials confirmed, unnamed |
| 253b | `memory3d` — **viscoelasticity in a volume** — the purest lift in the series: `p₀ ← μ·p_prev` is a per-cell scale, so unlike 251b (whose red-black parity had to become `(x+y+z)&1`) or 256 (which has no 3D scene at all, because a raymarch erases its artifact) there is *nothing structural to re-derive* — one kernel that never reads a neighbour, which is also why in-place is safe. μ>1 is genuine positive feedback and the kernel rails the magnitude so the self-exciting range stays explorable rather than needing a relaunch. Reference is deep multigrid with memory off. **Built as the law's 3D test case** — a scaled pressure field is smooth by construction and rides inside the flow, so [log §1](docs/artifacts-log.md) predicts it reads as a material where plaid's lattice-pinned checkerboard could not. | [artifacts-250](docs/artifacts-250.md) · [log §1](docs/artifacts-log.md) | ✅ built · not yet judged |
| 257b | `eq3d` — **a designed residual spectrum, in a volume** — the base plan defers this ("257 eq needs a 3D FFT — defer; 2D is the lab") on the assumption that the transform is the obstacle. It isn't: **the cosine transform is separable**, so three axes is three passes and not a new algorithm — six pipelines from one source (`f3_dct_1d.glslinc`), seven dispatches a frame. One thing genuinely does not lift by inspection and it is the whole content of the port: **the box is `N × 1.5N × N`, so each axis must be normalised by its OWN length when forming κ** — use a single N and the filter tilts, a mode of the same physical wavelength lands at a different κ depending which way it runs, and the knee becomes direction-dependent (an anisotropic material by accident; 255 grain wants that on purpose, this scene does not). **The only 3D scene in the series whose reference is not an approximation**: 250b and 251b both toggle to a multigrid V-cycle, whereas `W(k)=1` here *is* the exact projection. Ladder 56/96/128 (~0.9 / 7.5 / 24 ms) against 257's 128/256/512. | [artifacts-250](docs/artifacts-250.md) · [log §5](docs/artifacts-log.md) | ✅ built · not yet judged |

| 252 | `crossfade` — **a dial between honest and squishy** — `p = α·p_exact + (1−α)·p_truncated`, two solves of the same divergence blended every tick (`fs_pressure_blend.glslinc`). The plan lists this as blocked on a 2D multigrid hookup; it is not, and the substitute is **better than the original** — 257's spectral solve is EXACT, so the honest end of the dial is the answer rather than a well-converged approximation of it. Blending two pressure fields is legitimate because the projection is LINEAR in the divergence: a convex combination of two valid solutions solves the same Poisson problem with a blended right-hand side, so every intermediate α is a real solve, not a cross-dissolve. Order of operations is load-bearing and easy to get backwards — the truncated solve runs FIRST so it still warm-starts from last frame's blended pressure (its memory is part of its character); the spectral solve runs second precisely because it is direct and no initial guess can contaminate it. Reversed, the Jacobi side would warm-start from the exact answer and the artifact would quietly vanish. | [artifacts-250](docs/artifacts-250.md) · [log §1a](docs/artifacts-log.md) | ✅ built · not yet judged |
| 255 | `grain` — **directional residual as anisotropic stiffness**, built as a FALSIFICATION TEST — ADI (`fs_pressure_adi.glslinc`): every line along one axis solved EXACTLY as a tridiagonal system (Thomas, one thread per line, a scratch texture for the modified super-diagonal) while the cross-axis neighbours are frozen, then the axes swap. Information crosses the whole domain instantly along the sweep and not at all across it, so truncating the alternation freezes that imbalance into the residual as stripes — a material stiffer one way than the other, from nothing but the order the solver visited things. **Ping-ponged rather than in-place**: parallel lines read their cross-axis neighbours, so updating in place would have thread j reading what threads j±1 are concurrently writing — and reading the cross terms from the previous iterate is what ADI actually specifies, so the race fix and the correct scheme are the same thing. **The log's §1 law predicts this scene FAILS** (grid-aligned, doesn't convect — plaid's shape). Two good outcomes: it reads as noise and the law survives a real attempt to break it, or it reads as a material and the law is wrong, which is worth more. | [artifacts-250](docs/artifacts-250.md) · [log §1a](docs/artifacts-log.md) | ✅ built · not yet judged |
| 258 | `ring` — **θ-scheme dissipation as reverb tail** — first scene off the fluid and onto the wave stamp. cn=0 backward-Euler is stable *because* it discards energy every step, so the tail dies at once (dead room); cn=1 Crank-Nicolson is symmetric in past and future, dissipates nothing, and is still unconditionally stable (live room). Same stability guarantee, opposite decay time, difference purely discretisation — a knob that already existed in `stamp_wave.glslinc` as a stability option, renamed as what it is. **The artifact view had to change shape here**: every Block A scene renders a residual, but a dissipative scheme leaves nothing behind, only something MISSING — so the readout is \|h−h_prev\|, the ringing energy itself (`stamp_energy.glslinc` via the new `FieldProbe` helper). Top-down lit-relief display, unlike all of Block A — the base's `SideView` switch exists for exactly this split. Reference is cn=1, the closest thing to no scheme error and honestly weaker than 257's or 259's. | [artifacts-250](docs/artifacts-250.md) | ✅ built · not yet judged |
| 259 | `smear` — **dispersion, measured against `r = c·t`** — **the only EXACT reference in either plan.** Every other A/B in the series compares a cheap solve against a better solve; here the answer is a closed form: a single impulse on a uniform medium expands as a circular front of radius `c·t` for all time. The scene draws that circle in `#E23D6D` straight over the simulated field, so the gap between ring and wavefront IS the discretisation error — measured, not inferred, nothing to converge and nothing to take on trust. The dial is SUBSTEPS, which makes the artifact knob and the accuracy knob the same knob: raise it and watch the front climb back onto the ring. Impulse injected once on the first substep only (repeating it per substep would scale the strike with the dial and make the A/B meaningless); single impulse never periodic forcing, since a second front would make "which one should sit on the ring?" ambiguous. Signed two-tone display — a dispersive wake is a small oscillation *behind* the front, and shading would show only bumps. | [artifacts-250](docs/artifacts-250.md) | ✅ built · not yet judged |
| 260 | `ghost` — **advection smearing and limiter chatter** — semi-Lagrangian is unconditionally stable because it interpolates, and every interpolation is a small low-pass; run it a few hundred frames and filaments dissolve into haze with nothing having gone wrong and no residual anywhere. MacCormack keeps the detail at the cost of a limiter that chatters where the correction wants to overshoot. **The harness is the design**: solid-body rotation is imposed (`fs_solid_rotation.glslinc`), not solved, and it is exactly divergence-free — so there is no pressure solve, no buoyancy, no viscosity and no wall effects, and the advection scheme is the *only* thing that can change the field. That turns "does it look mushy?" into a real measurement, because after one full revolution the dye must be IDENTICAL to turn 0. The test pattern is seeded once and only rotated thereafter — re-injecting would hide the effect, since fresh dye is always sharp. Artifact view is \|∇dye\| (`stamp_grad.glslinc`): the gradient is the quantity being eaten. | [artifacts-250](docs/artifacts-250.md) | ✅ built · not yet judged |

| 290 | `slice` — **two-phase water/air in a vertical cross-section** — the first step toward a wave that can actually curl, and a deliberate departure from every shore scene before it: 26–33 and 50–52 are all height fields, depth-averaged and single-valued, and a curling wave has **three water surfaces above one x**. `h(x)` can steepen without limit and can never overturn — that is outside what those solvers represent, not a resolution problem. So ρ becomes a volumetric field: water and air as ONE scalar with a large ratio, no per-phase branching (the same observation `f3_add_gravity` makes for the 3D liquid). Gravity is **uniform on every cell including air** — the variable-density momentum equation carries no density factor on `g`, and the difference enters entirely through `1/ρ` on the pressure gradient. The projection is the **stamp contract applied to two-phase flow** (`fs_pressure_rho.glslinc`): `st_conductance = 1/ρ_face`, `st_diag = Σ`, `st_rhs = ∇·v/dt` — a weighted Laplacian, the same operator shape `stamp_wave_batty` uses for solid fractions, so one face can eventually carry both weights. **The artifact is spurious hydrostatic currents**: still water under still air has an exact answer (nothing moves), so every velocity at the waterline is discretisation error — and unlike every Block A artifact it does NOT converge away, because it is an inconsistency between how gravity is applied and how the pressure gradient is taken, not a truncation. | [shore-slice](docs/shore-slice.md) | ✅ verified (live) — flat waterline holds |

| 290b | `slice_big` — **the same slice at beach scale, under real gravity** — 80 m × 20 m at ~0.078 m/cell, `g = 9.81 m/s²`, which is 12× the effective force of 290's box and the configuration that previously boiled within seconds. Getting here cost **three fixes and two wrong hypotheses**, and the wrong ones looked identical on screen to the right one — which is why the scene carries a 1 Hz readback of `|v|max`, `\|div\|max`, ρ range, interface thickness and pressure at three heights. The detonator was weighting the pressure gradient by the **cell's** `1/ρ` instead of the **face's**: at the interface that applies `1/ρ_air` to a pressure difference belonging to the water below, a ~50× spurious kick every frame (`\|v\|max` 6.5×10⁷ → 0.6). Also required: solid-wall divergence in **flux form**, so `Σ div = 0` and the all-Neumann problem is actually solvable, and **hydrostatic pressure seeded in closed form** so the domain-scale ramp is handed to the solver rather than discovered by it. **Verified:** `p bot/mid/top = 11.39 / 2.19 / −0.002` against an analytic `11.18 / 2.22 / 0`. The lesson worth keeping: the 6 m box "worked" all along only because gravity was too weak to expose the formulation error — **a small test domain can hide one completely.** | [shore-slice](docs/shore-slice.md) | ✅ verified (live, measured) |

## Relationship to water-kit

`water-kit` stays GDScript (fast iteration, no build step — right for tiny
one-file demos). This repo takes on the techniques where C#'s structs, interfaces,
static typing, and speed pay off. Shaders (`.gdshader`) are portable between the
two verbatim.
